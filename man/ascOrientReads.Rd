% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/DsASC-tfactivity.R
\name{ascOrientReads}
\alias{ascOrientReads}
\title{Orient allele counts toward the predicted stronger-binding allele}
\usage{
ascOrientReads(refV, altV, delta)
}
\arguments{
\item{refV, altV}{integer vectors of reference / alternative counts (aligned).}

\item{delta}{numeric vector, PWM score difference \code{alt - ref}.}
}
\value{
list(H, L) oriented count vectors.
}
\description{
Given reference / alternative counts and a per-site PWM binding delta
(\code{delta = score(ALT) - score(REF)}), returns the read counts on the
predicted "high-binding" (\code{H}) and "low-binding" (\code{L}) allele.
Sites with \code{delta > 0} bind better on the ALT allele, so \code{H = alt};
sites with \code{delta < 0} bind better on REF, so \code{H = ref}.
}
\author{
Irem B. GUNDUZ
}
