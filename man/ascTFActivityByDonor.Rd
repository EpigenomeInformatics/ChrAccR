% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/DsASC-tfactivity.R
\name{ascTFActivityByDonor}
\alias{ascTFActivityByDonor}
\title{Allele-specific TF activity computed within each donor}
\usage{
ascTFActivityByDonor(
  refMat,
  altMat,
  deltaDt,
  annot,
  condition,
  stimCol = "stimulus",
  minDonorSamples = 1L,
  ...
)
}
\arguments{
\item{refMat, altMat}{count matrices [snp x sample].}

\item{deltaDt}{motif delta table (snpId, tf, delta); subset to the TFs
of interest beforehand to keep this fast.}

\item{annot}{sample annotation with \code{sampleId}, \code{donor} and
the condition column.}

\item{condition}{value of \code{stimCol} to select (e.g. "U" or "S").}

\item{stimCol}{name of the condition column (default "stimulus").}

\item{minDonorSamples}{minimum samples a donor must have in the condition
(default 1).}

\item{...}{passed to \code{ascTFActivity} (prefDelta, minReads,
minSites, nPerm, ...).}
}
\value{
data.table with per-(TF, donor) activity plus \code{donor},
        \code{condition}; NULL if nothing computable.
}
\description{
Repeats \code{\link{ascTFActivity}} separately for each donor's samples in a
given condition, so a pooled result can be checked for donor robustness (i.e.
that it is not driven by one deep donor). Per-donor coverage is lower, so some
TFs will drop below \code{minSites} in some donors -- that is expected and is
part of the robustness read-out.
}
\author{
Irem B. GUNDUZ
}
