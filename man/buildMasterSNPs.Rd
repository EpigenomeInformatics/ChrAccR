% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/import_asc.R
\name{buildMasterSNPs}
\alias{buildMasterSNPs}
\title{Build a shared master heterozygous-SNP list across donors}
\usage{
buildMasterSNPs(donors, vcfDir, genome)
}
\arguments{
\item{donors}{Character vector of donor IDs.}

\item{vcfDir}{Directory with \code{\{donor\}_heterozygous.vcf.gz} files.}

\item{genome}{Genome assembly string (e.g. "hg38").}
}
\value{
A GRanges with REF, ALT, snpId, donorMembership metadata; names() = snpId.
}
\description{
Reads donor VCFs once and returns a sorted, de-duplicated GRanges of
biallelic heterozygous SNPs spanning all requested donors. The returned
object carries, per site, the set of donors heterozygous there
(\code{$donorMembership}, a CharacterList), so the donor mask can be applied
later without re-reading VCFs. Save this once and pass it to
\code{DsASC.gatk(..., masterGr = )} in each per-cell-type job to keep all
objects row-aligned.
}
