% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/DsASC-tfactivity.R
\name{ascTFActivity}
\alias{ascTFActivity}
\title{Compute allele-specific TF activity for a set of samples}
\usage{
ascTFActivity(
  refMat,
  altMat,
  deltaDt,
  sampleIds,
  prefDelta = 1,
  minReads = 4L,
  minSites = 10L,
  nPerm = 2000L,
  seed = 42L,
  label = NA_character_
)
}
\arguments{
\item{refMat, altMat}{count matrices [snp x sample] (e.g. from
\code{mergeDsASCArray}).}

\item{deltaDt}{data.table with columns \code{snpId}, \code{tf},
\code{delta} (PWM score alt - ref), as produced by the
motif-scoring step of the driver script.}

\item{sampleIds}{samples to pool over (a condition, lineage, etc.).}

\item{prefDelta}{|delta| threshold to count a site as motif-disrupting
(default 1.0, matching 03_tf_binding.R).}

\item{minReads}{minimum pooled coverage (H+L) to keep a site (default 4).}

\item{minSites}{minimum number of usable sites to report a TF (default 10).}

\item{nPerm}{permutations for the null (default 2000).}

\item{seed}{RNG seed (default 42).}

\item{label}{optional context label copied into the output column
\code{context}.}
}
\value{
data.table: tf, context, nSites, obs, z, p, fdr (BH over TFs),
        meanNull, sdNull.
}
\description{
Pools reads across the requested samples, orients each ASC SNP toward its
predicted stronger-binding allele using \code{deltaDt}, and computes the
\code{\link{ascActivityStat}} per transcription factor. Only sites whose
absolute PWM delta exceeds \code{prefDelta} (i.e. the motif is actually
disrupted) and whose pooled coverage reaches \code{minReads} are used.
}
\author{
Irem B. GUNDUZ
}
