% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/DsASC-tfactivity.R
\name{ascTFActivitySwitch}
\alias{ascTFActivitySwitch}
\title{Allele-specific TF activity across conditions, with a switch (handoff) score}
\usage{
ascTFActivitySwitch(
  refMat,
  altMat,
  deltaDt,
  samplesA,
  samplesB,
  labelA = "A",
  labelB = "B",
  ...
)
}
\arguments{
\item{refMat, altMat}{count matrices [snp x sample].}

\item{deltaDt}{motif delta table (snpId, tf, delta).}

\item{samplesA, samplesB}{sample vectors for the two contexts.}

\item{labelA, labelB}{context labels (default "A"/"B").}

\item{...}{passed to \code{ascTFActivity} (prefDelta, minReads, ...).}
}
\value{
data.table with per-TF activity in both contexts plus deltaObs, flip.
}
\description{
Runs \code{\link{ascTFActivity}} separately in two contexts (e.g. resting vs
stimulated) and joins them, adding the change in activity
\code{deltaObs = obs_B - obs_A} and a \code{flip} flag for TFs whose activity
changes sign between contexts. These are candidate "dynamic TF handoffs".
}
\author{
Irem B. GUNDUZ
}
